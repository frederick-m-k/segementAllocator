import { CheckTextGridDirective } from './check-text-grid.directive';

describe('CheckTextGridDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckTextGridDirective();
    expect(directive).toBeTruthy();
  });
});
