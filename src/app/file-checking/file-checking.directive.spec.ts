import { FileCheckingDirective } from './file-checking.directive';

describe('FileCheckingDirective', () => {
  it('should create an instance', () => {
    const directive = new FileCheckingDirective();
    expect(directive).toBeTruthy();
  });
});
